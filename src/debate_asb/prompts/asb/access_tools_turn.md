You have read-only access to the research paper (`PAPER.md`) and the complete codebase that generated its results, through three tools: `list_files`, `read_file` and `search`. You cannot run code, and you cannot view images. You can make up to {max_steps} rounds of tool calls per turn. Search first and read only the line ranges you need: reading whole large files will overflow your context window.
