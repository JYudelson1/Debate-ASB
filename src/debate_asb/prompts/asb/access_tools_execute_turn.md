You have access to the research paper (`PAPER.md`) and the complete codebase that generated its results, through four tools: `list_files`, `read_file` and `search`, which read the codebase, and `run_bash`, which runs a bash command in a sandboxed copy of the codebase at `/workspace`. `/workspace` is read-only: to modify code, copy it to your home directory (`~`) first. Other participants use the same sandbox, so files in your home directory may not be yours. Each command is killed after 60 seconds. The sandbox has no network access and no GPU. It has Python 3.12 with common ML packages installed CPU-only, including torch and transformers; run `pip list` to see exactly what is installed. Nothing else can be installed. You cannot view images. You can make up to {max_steps} rounds of tool calls per turn.

Do NOT run experiments: no training scripts, experiment pipelines, or anything that trains models or generates new results. Run code to inspect data, test individual functions, and check saved outputs.
